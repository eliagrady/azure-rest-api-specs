// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

using System;
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using System.Threading.Tasks;
using Azure;
using Azure.Core;
using Azure.Identity;
using NUnit.Framework;

namespace SecurityCenter.Samples
{
    public class Samples_Client
    {
        [Test]
        [Ignore("Only validating compilation of examples")]
        public void Example_GetOperations()
        {
            var credential = new DefaultAzureCredential();
            var client = new Client(credential);

            foreach (var item in client.GetOperations(new RequestContext()))
            {
                JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
                Console.WriteLine(result.ToString());
            }
        }

        [Test]
        [Ignore("Only validating compilation of examples")]
        public void Example_GetOperations_AllParameters()
        {
            var credential = new DefaultAzureCredential();
            var client = new Client(credential);

            foreach (var item in client.GetOperations(new RequestContext()))
            {
                JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
                Console.WriteLine(result.GetProperty("name").ToString());
                Console.WriteLine(result.GetProperty("origin").ToString());
                Console.WriteLine(result.GetProperty("display").GetProperty("provider").ToString());
                Console.WriteLine(result.GetProperty("display").GetProperty("resource").ToString());
                Console.WriteLine(result.GetProperty("display").GetProperty("operation").ToString());
                Console.WriteLine(result.GetProperty("display").GetProperty("description").ToString());
            }
        }

        [Test]
        [Ignore("Only validating compilation of examples")]
        public async Task Example_GetOperations_Async()
        {
            var credential = new DefaultAzureCredential();
            var client = new Client(credential);

            await foreach (var item in client.GetOperationsAsync(new RequestContext()))
            {
                JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
                Console.WriteLine(result.ToString());
            }
        }

        [Test]
        [Ignore("Only validating compilation of examples")]
        public async Task Example_GetOperations_AllParameters_Async()
        {
            var credential = new DefaultAzureCredential();
            var client = new Client(credential);

            await foreach (var item in client.GetOperationsAsync(new RequestContext()))
            {
                JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
                Console.WriteLine(result.GetProperty("name").ToString());
                Console.WriteLine(result.GetProperty("origin").ToString());
                Console.WriteLine(result.GetProperty("display").GetProperty("provider").ToString());
                Console.WriteLine(result.GetProperty("display").GetProperty("resource").ToString());
                Console.WriteLine(result.GetProperty("display").GetProperty("operation").ToString());
                Console.WriteLine(result.GetProperty("display").GetProperty("description").ToString());
            }
        }
    }
}
